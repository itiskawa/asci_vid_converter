# Video to ASCII Clip converter

This program takes an entry as a video clip (all standard formats accepted by OpenCV), and returns an .avi file of ASCII art.

Libraries Used: 
- PIL
- OpenCV
- FFMPEG
- NumPy
- Os



Stilla bug on the ffmpeg Stream, .mp4 doesn't work
